🧹 Remove verbose WebLLM engine initialization log

🎯 **What:** Removed the unnecessary `console.log("WebLLM Engine initialized.");` from `src/engine/GraphRAG.ts`.
💡 **Why:** This reduces unnecessary console noise, which improves logging clarity, especially during application startup or when WebLLM starts multiple times.
✅ **Verification:** I've run `npm run typecheck`, `npm run lint` and `npm run test` successfully.
✨ **Result:** A slightly cleaner codebase and quieter console output on startup.
