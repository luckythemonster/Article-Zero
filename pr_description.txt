🧪 Add tests for jsfxr dump parsing error paths

🎯 **What:** Adds missing tests to cover error paths when parsing JSFXR dump lines, specifically the catch block in `parseJsfxrDump`.

📊 **Coverage:** Now tests the behavior when validly formatted blocks sit alongside malformed blocks (which should be skipped without throwing exceptions) and validates that perfectly formed sets still parse appropriately.

✨ **Result:** Improved test coverage for audio file parsing and resilience against malformed JSFXR inputs, increasing overall codebase reliability.
